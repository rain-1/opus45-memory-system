# Opus Memory Discord Bot - Environment Configuration
# Copy this to .env and fill in your values
# ⚠️  DO NOT COMMIT .env - it contains API keys!

# Discord Configuration
# ====================

# Required: Discord bot token
# Get this from https://discord.com/developers/applications
DISCORD_TOKEN=your_discord_bot_token_here

# Required: Anthropic API key
# Get this from https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: Path to store memories (default: ./opus_memories)
OPUS_MEMORY_PATH=./opus_memories

# Optional: Model to use (default: claude-sonnet-4-20250514)
# Options: claude-sonnet-4-20250514, claude-opus-4-20250514, claude-haiku-4-5
OPUS_MODEL=claude-haiku-4-5


# Trust & Memory Control
# ======================

# Operator IDs - Discord user IDs who can access memory commands
# Get user IDs by enabling Developer Mode in Discord, then right-click user -> Copy ID
# Comma-separated list
OPUS_OPERATOR_IDS=123456789012345678,987654321098765432

# Only learn/extract memories from operator conversations (default: true)
# Set to false to learn from designated channels too
OPUS_LEARN_FROM_OPERATORS_ONLY=true

# Channels to learn from (only used if LEARN_FROM_OPERATORS_ONLY=false)
# Comma-separated channel names
OPUS_LEARN_CHANNELS=

# Enable memory admin commands for operators (default: true)
# Even when false, operators' conversations still contribute to memory
OPUS_MEMORY_COMMANDS=true

# Cross-Channel Context Awareness
# ================================

# Enable cross-channel context awareness (default: true)
# When enabled, the bot can reference discussions from other channels in the same server
OPUS_CROSS_CHANNEL_CONTEXT=true

# Number of cross-channel memories to retrieve per response (default: 5)
# Higher values provide more context but use more tokens
OPUS_SERVER_CONTEXT_MEMORIES=5


# GitHub Integration (for self-improvement loop)
# ===============================================

# Required: GitHub personal access token with repo access
# Create at https://github.com/settings/tokens
# ⚠️  This is sensitive - never commit this!
GITHUB_TOKEN=your_github_personal_access_token_here

# Required: GitHub repository owner (username or organization)
GITHUB_REPO_OWNER=rain-1

# Required: GitHub repository name
GITHUB_REPO_NAME=opus45-memory

# Optional: Label for issues to auto-fix (default: auto-fix)
GITHUB_AUTO_FIX_LABEL=auto-fix

# Optional: Label identifying opus-bot-created issues (default: opus-bot)
GITHUB_BOT_USER_LABEL=opus-bot